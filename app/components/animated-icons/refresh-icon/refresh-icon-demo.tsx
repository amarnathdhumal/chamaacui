"use client";

import RefreshIcon from "@/registry/chamaac/animated-icons/refresh-icon";

export default function RefreshIconDemo() {
  return (
    <RefreshIcon
      className="text-black dark:text-white"
      size={40}
      duration={2}
      strokeWidth={2}
    />
  );
}
